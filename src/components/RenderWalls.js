import React, { Component } from 'react';
import AddWallForm from './AddWallForm';
import waterfalls from '../lib/waterfalls';

class RenderWalls extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }

  render() {
    return (
      <div className="RenderWalls">
        <header className="RenderWalls-header">
        </header>

      </div>
    );
  }
}

export default RenderWalls;
